apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodeapp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "nodeapp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodeapp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
